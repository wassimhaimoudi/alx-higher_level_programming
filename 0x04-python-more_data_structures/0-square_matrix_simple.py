#!usr/bin/python3
def square_matrix_simple(matrix=[]):
    """
    A function that computes the square
    value of all integers in a matrix
    """
    new_matrix = []
    for row in matrix:
        squared = list(map(lambda x : x**2, row))
        new_matrix.append(result)
    return (new_list)
